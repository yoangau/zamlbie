type entity_type = [ Player ]

type entity =
  { entity_type : entity_type;
    id : int;
    x : int;
    y : int
  }

type game =
  { entities : entity list;
    width : int;
    height : int
  }

type move =
  [ Up
  | Down
  | Left
  | Right
  ]
