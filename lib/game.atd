type player_type = [ Ally | Enemy ]

type entity_type = [ Player of player_type ]

type entity =
  { entity_type : entity_type;
    id : int;
    x : int;
    y : int
  }

type config =
  { view_radius_ally : int;
    view_radius_enemy : int;
    width : int;
    height : int;
    player_count : int;
    time_limit : int;
    tick_rate : float
  }

type game =
  { entities : entity list;
    config : config
  }

type move =
  [ Up
  | Down
  | Left
  | Right
  ]
